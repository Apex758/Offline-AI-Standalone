# ========================================
# OECS Learning Hub - Backend Dependencies
# ========================================

# --- API / Backend ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiohttp>=3.9.0

# --- LLMs ---
llama-cpp-python>=0.2.0
google-genai>=0.3.0

# --- Image Generation & Editing ---
# ⚠️ Install iopaint FIRST, then optimum-intel
# Let iopaint manage diffusers/huggingface-hub/transformers versions
iopaint==1.6.0
optimum-intel[openvino]
Pillow>=9.5.0
requests>=2.31.0

# --- Document / Rendering ---
python-docx>=1.0.0
weasyprint>=60.0

# ========================================
# INSTALLATION ORDER
# ========================================
# pip install iopaint==1.6.0
# pip install optimum-intel[openvino]
```

---

## 🎯 **What Will Happen:**
```
iopaint==1.6.0 installs:
├── diffusers==0.27.2
├── huggingface-hub==0.25.2
├── transformers==4.48.3
├── accelerate (latest compatible)
└── ... (all its dependencies)

Then optimum-intel[openvino] installs:
├── optimum (latest that works with transformers 4.48.3)
├── openvino
├── nncf
└── ... (adapts to existing versions)

Result: ✅ NO CONFLICTS
```

---

## ⚠️ **Why Previous Commands Failed:**
```
❌ What I said:
pip install huggingface-hub==0.20.3  # Too old for iopaint
pip install transformers==4.38.0     # Too old for iopaint
pip install optimum==1.18.0          # Specific version

✅ What should happen:
pip install iopaint==1.6.0           # Installs 0.25.2, 4.48.3
pip install optimum-intel[openvino]  # Adapts automatically