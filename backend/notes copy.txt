
Here's what's weighing down your app, ranked by impact:

Critical Issues
Huge image assets (~16.5 MB) — 230+ PNGs in dist, some 2-3 MB each (belonging.png 3.1 MB, authors-workshop.png 2.3 MB). Convert to WebP and lazy-load them.

1.1 MB curriculumIndex.json bundled directly into the frontend. Split it into chunks and lazy-load on demand.

Heavy libraries imported eagerly:

framer-motion — heavy animation lib; CSS transitions would suffice for most uses
recharts — full charting lib, only used in AnalyticsDashboard
pptxgenjs, xlsx, html2canvas — export libs loaded upfront instead of on-demand
ogl — WebGL lib for Galaxy component, rarely used
No route-level code splitting — only Dashboard uses React.lazy(). Every other route is eagerly imported, bloating the initial bundle (4-6 MB total JS chunks, with individual chunks up to 705 KB).

Major Issues
Giant monolithic components — ImageStudio.tsx (3,028 LOC), PresentationBuilder.tsx (2,913 LOC), Settings.tsx (2,562 LOC). These cause excessive re-renders with 725+ useState hooks across the app and almost no useMemo/useCallback/React.memo.

39+ icon imports scattered across Dashboard.tsx from @hugeicons/core-free-icons — consider a centralized icon registry.

Electron main process blocks on startup — synchronous file operations (copyDirectorySync) in electron/main.js block the main thread during model setup.

Quick Wins (highest ROI)
Fix	Estimated Gain
Dynamic-import export libs (pptxgenjs, xlsx, html2canvas, recharts)	~40% bundle reduction
Convert PNGs to WebP + lazy load images	~60-70% asset reduction
React.lazy() on all routes	~15% faster initial load
Split/lazy-load curriculumIndex.json	~25% faster initial load
Add React.memo() to list components	20-30% fewer re-renders
Replace framer-motion with CSS transitions	~10% bundle reduction
Want me to start tackling any of these?