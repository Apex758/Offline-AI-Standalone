# ========================================
# OECS Learning Hub - Backend Dependencies
# ========================================

# --- API / Backend ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiohttp>=3.9.0

# --- LLMs ---
llama-cpp-python>=0.2.0

# --- Image Generation & Editing ---
# Let iopaint manage diffusers/huggingface-hub/transformers versions
iopaint==1.6.0
optimum-intel[openvino]
Pillow
requests>=2.31.0

# --- Document / Rendering ---
python-docx>=1.0.0
weasyprint>=60.0
beautifulsoup4==4.14.2
soupsieve==2.8

# ========================================
# NOTES
# ========================================
# - iopaint 1.6.0 requires diffusers 0.27.2
# - optimum-intel 1.18.0 supports diffusers 0.27.2
# - huggingface-hub 0.20.3 works with optimum 1.18.0
# - This combo is tested and stable

# ========================================
# MANUAL INSTALLATION REQUIRED
# ========================================
# llama-cpp-python must be installed separately:
# pip install llama-cpp-python --no-build-isolation --config-settings=builddir=build

# ========================================
# MANUAL INSTALLATION REQUIRED
# ========================================
# llama-cpp-python must be installed separately:
# pip install llama-cpp-python --no-build-isolation --config-settings=builddir=build
# pip install llama-cpp-python --no-build-isolation --config-settings=builddir=build