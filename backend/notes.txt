python -m uvicorn main:app --reload --port 8000



.\setup-embedded-python.bat


.\python-embed\python.exe build-scripts/package-backend.py


.\build-release.ps1


.............................    use uvicorn main:app --reload to start backend


Next steps:


-Implement  rich text editor after generation 
-Convert curriculum to vectors in FAISS
-Connect the curriculum with the lesson planners.
-Connect AI Assistant

-Better organize curriculum pages and develop map to feed into ai

-Create options for LLM

-Apply LoRA patches 

-Update Tab Names with current content

-Clean up code

-Add walkthrough element

-FIX PROMPTS

-settings for updates



-use api key for powerful model while the user is connected to the internet

-read up on the email ashley sent


add timer to show estimated time of completetion based on pc specs
ensure timing allocation is right for all gener
ensure editors match
 include marks for rubric gen
 after logging in, create name/oak fields to enter




 cd "C:\Users\LG\Desktop\Offline AI Standalone"

# 1. Run the fixed packaging script
.\manual-package.ps1

# 2. Rebuild Electron
npm run electron:build:win

# 3. Copy models
Copy-Item -Path "models\*" -Destination "dist-electron\win-unpacked\resources\models" -Recurse -Force

# 4. Test manually first
cd "dist-electron\win-unpacked\resources\backend-bundle"
.\python-embed\python.exe start_backend.py


Use .\build-release.ps1 for production releases.

Use npm run electron:build:win only for local Electron packaging tests.














Tuning guide:

Small model (2GB): max_turns=2 (remembers last 2 exchanges)
Medium model (4GB): max_turns=4 (remembers last 4 exchanges)
Large model (8GB+): max_turns=6 (remembers last 6 exchanges)